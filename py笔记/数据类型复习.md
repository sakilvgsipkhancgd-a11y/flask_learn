Python包含众多数据类型有：整数int，浮点数float，布尔值bool，复数complex，字符串str，列表list，元组Tuple，集合set，字典dict
其中4个数字类型以及字符串和元组为不可变类型，列表，字典，集合为可变类型，内置函数type()可以查看数据类型
需要注意，python3中，布尔值True和Flase实际上是int的子类，可以和数字相加，True=1，Flase=0



